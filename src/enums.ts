export enum Visibility {
  PUBLIC,
  PRIVATE,
}
export enum StandardMetrics {
  DISTANCE, // m
  TIME, // s
  STROKE_RATE, //strokes/min
}
export enum ResultTypes {
  DISTANCE, // m
  TIME, // s
}
export enum ObjectiveTypes {
  DISTANCE, // m
  TIME, // s
}
export enum ChallengeStatus {
  DRAFT,
  PENDING,
  UNDERWAY,
  COMPLETED,
}
export enum InviteStatus {
  PENDING,
  ACCEPTED,
  DECLINED,
}
export enum DashboardDisplayOptions {
  EntryFeed,
  Challenges,
  PersonalProfile,
}
